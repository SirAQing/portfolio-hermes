# Frontend production environment — used during `npm run build`
# Set this in Cloudflare Pages dashboard > Settings > Environment variables.
# DO NOT commit real values here; keep as placeholder.
VITE_API_BASE=https://your-domain.com
